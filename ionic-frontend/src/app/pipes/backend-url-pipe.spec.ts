import { BackendUrlPipe } from './backend-url-pipe';

describe('BackendUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new BackendUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
